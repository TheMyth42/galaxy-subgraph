{
  "name": "galaxy-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ galaxy-subgraph",
    "create-local": "graph create --node http://localhost:8020/ galaxy-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ galaxy-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 galaxy-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.64.0",
    "@graphprotocol/graph-ts": "0.64.0"
  }
}
